
/**
 * Use this config sample to configure find-devs
 * These constants will vary from user to user.
 */

var config = {};

// Goto https://github.com/settings/tokens/new to get yourself your own tokens.
config.GITHUB_TOKEN  = '123abc';

module.exports = config;
